# PostgreSQL
DB_HOST=localhost
DB_NAME=flight_weather
DB_USER=postgres
DB_PASSWORD=your_password_here

# Prefect Cloud (required only for cloud deployment, optional for local)
PREFECT_API_KEY=your_prefect_cloud_api_key
PREFECT_API_URL=https://api.prefect.cloud/api/accounts/<id>/workspaces/<id>
